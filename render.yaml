services:
  - type: web
    name: webdashboard
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: npm run preview
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
